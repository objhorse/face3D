
**医美级高保真 3D 人脸重建与皮肤分析系统**

## **项目设计文档 (初稿)**

### **1\. 项目愿景 (Project Vision)**

本系统旨在通过三个固定角度（正脸、左侧45°、右侧45°）的 2D 影像，快速构建具有**物理级精度**和**照片级皮肤纹理**的 3D 人脸模型。系统不仅提供全方位视角查看，更集成了医美深层皮肤诊断功能（色沉、红区、皱纹），为术前评估和术后对比提供数字化底座。

### ---

**2\. 核心技术路线：3DMM \+ 深度融合方案**

选择 **3DMM (3D Morphable Model)** 作为核心，主要基于其在稀疏视角下的稳健性。

* **几何层 (Geometry):** 利用 3DMM 的统计先验（如 FLAME 或 BFM 模型）保证基础脸型的准确。  
* **细节层 (Details):** 结合物理深度估计，补偿 3DMM 无法表达的个性化细节（如痘坑、肿胀）。  
* **纹理层 (Texture):** 采用多视角无缝融合技术，还原真实皮肤质感及临床瑕疵。

### ---

**3\. 系统架构 (System Architecture)**

系统分为四个核心模块：数据采集、几何重构、纹理映射、临床诊断。

#### **3.1 数据采集与预处理 (Data Acquisition)**

* **输入:** 三路同步/异步高清 2D 图像（正、左、右）。  
* **自校准:** 由于无预设参数，系统需通过 **MediaPipe Face Mesh** 提取 468 个关键点，利用 **PnP 算法** 结合 3DMM 模板反推相机位姿。  
* **语义分割:** 自动分割皮肤、五官、头发及背景，仅保留有效皮肤区域进行建模。

#### **3.2 3DMM 几何重构 (3DMM Reconstruction)**

* **模型拟合:** 使用 **DECA** 或 **3DDFA-V2** 算法，将 3DMM 参数（形状、表情、姿态）与 2D 关键点匹配。  
* **置换贴图 (Displacement Mapping):** 调用 **Depth-Anything-V2** 生成高分辨率深度图，将其作为微观偏移量作用于 3DMM Mesh 顶点，还原视频中可见的细微起伏。  
* **尺度对齐:** 根据瞳距（PD）自动缩放模型至真实物理尺寸（毫米级）。

#### **3.3 高保真纹理融合 (Seamless Texture Mapping)**

* **UV 展开:** 将 3D 脸部展开为标准的 UV 坐标。  
* **多视角融合:** \* **权重投影:** 依据面片法线与相机的夹角，动态分配三张照片的贡献度。  
  * **接缝处理:** 使用**拉普拉斯金字塔融合**或**泊松融合**，消除侧脸与正脸交界处的色差和亮度跳变。  
* **去光影处理 (Delighting):** 通过 AI 算法减弱环境闪光灯产生的强高光，恢复皮肤本色（Albedo）。

#### **3.4 皮肤临床分析层 (Diagnostic Layers)**

视频中展示的核心诊断逻辑：

* **红区检测 (Erythema):** 提取 Lab 空间的 $a$ 通道，量化毛细血管扩张及炎症。  
* **色沉分析 (Pigmentation):** 采用数字滤波（如 RBX 技术逻辑）分离黑色素，定位斑点和潜在斑。  
* **皱纹/纹理 (Wrinkles):** 基于几何曲率和纹理对比度提取细纹深度。

### ---

**4\. 技术栈推荐 (Tech Stack)**

| 维度 | 推荐工具/库 |
| :---- | :---- |
| **编程语言** | Python 3.10+ (后端), TypeScript (前端) |
| **深度学习框架** | PyTorch |
| **几何计算** | Open3D, Trimesh, SciPy |
| **人脸先验** | FLAME Model / MediaPipe |
| **深度估计** | Depth-Anything-V2 / Marigold |
| **3D 渲染展示** | Three.js / React-Three-Fiber |

### ---

**5\. 预期效果 (Expected Outcomes)**

根据视频演示，系统需达到以下指标：

1. **还原度:** 皮肤纹理分辨率不低于 2K，色斑、毛孔位置偏移小于 1mm。  
2. **交互性:** 支持 Web 端 720° 旋转、局部放大、多模式切换（皮肤、红区、纹理）。  
3. **时效性:** 单人建模总时长控制在 30-60 秒内，符合医美诊室即拍即看的流程。

### ---

**6\. 后续扩展 (Future Roadmap)**

* **体积计算:** 计算填充物（如玻尿酸）注射前后的容积变化（cc）。  
* **衰老模拟:** 基于 3D 骨架的皮肉下垂模拟，用于抗衰项目展示。  
* **VR/AR 预览:** 医生直接在 3D 模型上进行画线规划。

### ---

**7\. 给 Claude Code 的执行建议**

1. **阶段一:** 实现 MediaPipe 到标准 3DMM Mesh 的拟合脚本。  
2. **阶段二:** 集成深度图，实现顶点的微观位移（Displacement）。  
3. **阶段三:** 编写多视角纹理融合逻辑（本项目最难点）。  
4. **阶段四:** 开发基于 Three.js 的前端加载器，支持切换不同的分析图层贴图。

---

**你可以直接把这份文档贴给 Claude Code，并告诉它：“按照这个架构，先帮我写出‘阶段一’和‘阶段二’的 Python 核心代码逻辑”。**