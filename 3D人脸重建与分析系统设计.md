# 医美级高保真 3D 人脸重建系统 — 设计文档（定稿）

## 1. 项目愿景

通过三个固定角度（正脸、左侧45°、右侧45°）的 2D 影像，快速构建具有**物理级精度**和**照片级皮肤纹理**的 3D 人脸模型。系统支持多光照纹理图层切换，并为术前术后评估提供档案管理与精准 3D 对比功能。

---

## 2. 核心技术路线：3DMM + 深度融合

选择 **3DMM (3D Morphable Model)** 作为核心，主要基于其在稀疏视角下的稳健性。

- **几何层：** 利用 FLAME/DECA 统计先验保证基础脸型准确，再通过联合多视角优化提升精度
- **细节层：** Depth-Anything-V2 深度图经尺度对齐后，作为顶点置换量补偿个性化细节
- **纹理层：** 多视角加权 UV 投影 + 泊松融合，还原真实皮肤质感

---

## 3. 系统架构

### 3.0 相机内参预测（新增）

- **输入：** 3 张人脸照片
- **方法：** Dust3R 预测内参矩阵 K
- **接口：** 预留手动覆盖接口，用户可 POST 真实内参（fx, fy, cx, cy）直接替换
  ```
  POST /api/calibration
  Body: { fx, fy, cx, cy }
  ```

### 3.1 数据采集与预处理

- **输入：** 每种光照条件下的 3 张同步照片（正、左45°、右45°）
  - 当前实现：白光（white），接口预留其他光照类型
  - 纯黑背景，同组照片同时拍摄（无微动）
- **关键点提取：** MediaPipe Face Mesh → 468 个关键点 × 3 视角
- **语义分割：** 黑背景条件下自动提取皮肤 Mask

### 3.2 联合 3DMM 几何重构

**初值策略：**
1. DECA 单图推理 × 3，分别得到每视角初始参数
2. 取形状参数均值作为联合优化初值

**联合优化：**
- 共享参数：形状 α（同一人骨架唯一）
- 独立参数：表情 β、位姿 R/t（每视角各自独立）
- 优化器：L-BFGS
- 损失函数：
  ```
  L = Σ_v ||proj(α, βv, Rv, tv) - kp_v||²  +  λ·||α||²
  ```

**深度置换：**
1. Depth-Anything-V2 → 相对深度图 D_pred
2. 渲染 3DMM Mesh → 伪深度图 D_3dmm
3. 最小二乘对齐：`D_aligned = s·D_pred + t`
4. 面法线与相机方向夹角作为权重，融合 3 视角置换量

**输出：** 精细 Mesh（带 UV 坐标，.obj）

### 3.3 多光照纹理映射

对每种光照的 3 张照片：
1. 计算各面片法线与对应相机方向的夹角 → 混合权重
2. 将 3 张图分别投影到 UV 空间 → 加权混合
3. 泊松融合消除视角接缝

**打包格式：** GLB（Draco 压缩），extras 元数据携带图层信息：
```json
{ "layers": [{ "name": "白光", "type": "white" }] }
```

### 3.4 服务层

| 接口 | 协议 | 说明 |
|------|------|------|
| 上传图片 / 触发重建 | REST (FastAPI) | 返回 session_id |
| 重建进度 | WebSocket | 阶段：内参→关键点→3DMM→深度→纹理→打包 |
| 查询/下载档案 | REST | 返回患者历史 session 列表及 GLB 下载链接 |
| 手动内参覆盖 | REST | `POST /api/calibration` |
| 术前术后对齐 | REST | `POST /api/compare` → 返回 4×4 变换矩阵（<10s） |

**数据库：** PostgreSQL（患者 ID / 日期 / session / 模型路径）
**文件存储：** 本地文件系统（GLB 文件）

### 3.5 术前术后对比

- **触发时机：** 用户点击"对比"按钮时才执行（懒惰计算）
- **方法：** 后端 ICP，将 post 模型对齐到 pre 模型坐标系
- **返回：** 4×4 变换矩阵（轻量），前端直接应用
- **前端交互：** 左右分屏同步旋转 / 单独显示（可切换）

---

## 4. 技术栈

| 维度 | 工具/库 |
|------|---------|
| 编程语言 | Python 3.10+（后端）/ TypeScript（前端） |
| 深度学习框架 | PyTorch（GPU） |
| 内参预测 | Dust3R |
| 关键点 | MediaPipe Face Mesh（468点） |
| 语义分割 | BiSeNet / MediaPipe Selfie |
| 3DMM 先验 | FLAME / DECA |
| 深度估计 | Depth-Anything-V2 |
| 几何计算 | Open3D, Trimesh, SciPy |
| 后端框架 | FastAPI + WebSocket |
| 数据库 | PostgreSQL |
| 3D 格式 | GLB + Draco |
| 前端渲染 | Three.js / React-Three-Fiber |

---

## 5. 预期效果

1. **还原度：** 皮肤纹理分辨率不低于 2K，色斑/毛孔位置偏移小于 1mm
2. **交互性：** Web 端 720° 旋转、局部放大、多光照图层切换
3. **时效性：** 单人建模总时长控制在 30-60 秒内
4. **对比：** 术前术后 ICP 对齐耗时 <10 秒，左右分屏同步旋转

---

## 6. 分阶段实现计划

| 阶段 | 内容 |
|------|------|
| 阶段一 | 模块0+1+2：内参预测 → 关键点提取 → 联合3DMM拟合 → 输出精细Mesh |
| 阶段二 | 模块2深度置换：Depth-Anything-V2 → 尺度对齐 → 顶点位移 |
| 阶段三 | 模块3纹理融合：多视角投影 → 泊松融合 → GLB打包（含lighting元数据） |
| 阶段四 | 模块4服务层：FastAPI + WebSocket + PostgreSQL档案管理 |
| 阶段五 | 前端：Three.js加载GLB + 图层切换 + 术前术后对比 |

---

## 7. 后续扩展（Roadmap）

- **体积计算：** 填充物注射前后的容积变化（cc）
- **衰老模拟：** 基于 3D 骨架的皮肉下垂模拟
- **VR/AR 预览：** 医生在 3D 模型上进行画线规划
- **多光照扩展：** UV 光、偏振光等图层（接口已预留）
